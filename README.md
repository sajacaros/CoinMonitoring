# CoinMonitoring
## 실행
```
$ coinTracker-backend
$ pip install -r requirements.txt
$ vi .env 			# config
$ uvicorn app.main:app --reload
```
## 실행
```
$ cd coinTracker-frontend
$ npm install
$ npm start
```